# Production Environment Variables for Firebase Deployment

# Backend API URL - Update this after deploying backend
# Option 1: If using Firebase Functions
VITE_API_URL=https://orionmaps.xyz/api/v1

# Option 2: If using separate backend service (Render, Railway, etc.)
# VITE_API_URL=https://api.orionmaps.xyz/api/v1

# Google OAuth Configuration - Get from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Mapbox Token (add if you have one)
# VITE_MAPBOX_TOKEN=your_mapbox_token_here
